slug: combination-calculator
category: math
applicationCategory: EducationalApplication
related: [factorial-calculator, statistics-calculator]
primaryKeyword: { en: "Combination calculator (nCr)", ja: "組み合わせ計算機 (nCr)" }
inputs: [{name: n, type: number}, {name: r, type: number}]
output: { type: number }
logic: "nCr = n!/(r!(n-r)!), computed multiplicatively; require 0<=r<=n integers. result keys: label, empty"
locales: all
