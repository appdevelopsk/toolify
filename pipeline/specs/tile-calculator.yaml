slug: tile-calculator
category: math
applicationCategory: UtilitiesApplication
related: [square-footage-calculator, paint-calculator]
primaryKeyword: { en: "Tile calculator", ja: "タイル枚数計算機" }
inputs: [{name: roomLength, type: number}, {name: roomWidth, type: number}, {name: tileSize, type: number}]
output: { type: number }
logic: "room m2 = roomLength_m*roomWidth_m; tile m2 = (tileSize_cm/100)^2; tiles = ceil(room/tile*1.1) incl 10% waste. result keys: label, empty"
locales: all
