slug: z-score-calculator
category: math
applicationCategory: EducationalApplication
related:
  - statistics-calculator
  - percentage-calculator
primaryKeyword:
  en: "Z-score calculator"
  ja: "Zスコア計算機"
inputs:
  - { name: value, type: number }
  - { name: mean, type: number }
  - { name: sd, type: number }
output: { type: number }
logic: |
  z = (value - mean) / sd; sd must be > 0
locales: all
