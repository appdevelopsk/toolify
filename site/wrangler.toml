name = "toolify"
compatibility_date = "2024-09-23"

# /api/price-search は runtime = "nodejs" を使用するため nodejs_compat が必須。
# CF Pages ダッシュボード → Settings → Functions → Compatibility flags でも同様に設定可能。
compatibility_flags = ["nodejs_compat"]
