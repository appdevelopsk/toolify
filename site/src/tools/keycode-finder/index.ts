import type { ToolMeta } from "@/lib/tools/types";

const meta: ToolMeta = {
  slug: "keycode-finder",
  category: "text",
  applicationCategory: "DeveloperApplication",
  updatedAt: "2026-05-18",
  related: ["url-encoder", "base64-encoder", "ascii-table"],
  primaryKeyword: { en: "keyboard key code finder", ja: "キーコード 調べる" },
  hasHowTo: true,
  hasFaq: true,
};

export default meta;
