import type { ToolMeta } from "@/lib/tools/types";

const meta: ToolMeta = {
  slug: "html-entity-encoder",
  category: "text",
  applicationCategory: "DeveloperApplication",
  updatedAt: "2026-05-18",
  related: ["url-encoder", "base64-encoder", "html-to-markdown"],
  primaryKeyword: { en: "HTML entity encoder", ja: "HTMLエンティティ 変換" },
  hasHowTo: true,
  hasFaq: true,
};

export default meta;
