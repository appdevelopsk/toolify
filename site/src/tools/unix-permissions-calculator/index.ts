import type { ToolMeta } from "@/lib/tools/types";

const meta: ToolMeta = {
  slug: "unix-permissions-calculator",
  category: "text",
  applicationCategory: "DeveloperApplication",
  updatedAt: "2026-05-18",
  related: ["base64-encoder", "json-formatter", "url-encoder"],
  primaryKeyword: { en: "unix permissions calculator", ja: "Unix パーミッション 計算" },
  hasHowTo: true,
  hasFaq: true,
};

export default meta;
